var name  = "Pramod";

var firstName = "Pramod";
var lastName = "Dutta"; //CamelCase

var first_name = "Amit"; //Snake Case

// PascalCase
var FirstName = "Rahul";
var LastName = "Sharma";

// kebab-case (not valid for JS variables, used in CSS/HTML)
// var first-name = "Invalid"; // This would cause a syntax error

// CONSTANT_CASE (uppercase with underscores)
var MAX_COUNT = 100;
var API_URL = "https://api.example.com";

// Hungarian notation (prefix indicating type)
var strName = "Vikram";
var intAge = 25;
var isActive = true;

// Single character identifiers
var i = 0;
var j = 0;
var x = 10;

// Dollar sign prefix (common in jQuery)
var $element = document.getElementById("myId");
var $_privateVar = "hidden";

// Underscore prefix (convention for private/internal variables)
var _internalCounter = 0;
var _tempValue = null;

// Mixed examples
var userNameList = []; // camelCase
var USER_NAME_LIST = []; // CONSTANT_CASE
var UserNameList = []; // PascalCase
var user_name_list = []; // snake_case